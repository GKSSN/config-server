server:
  port: 9096
  
spring:
 application:
    name: CONFIG-SERVER
 cloud:
  config:
   server:
    git:
     uri: https://github.com/GKSSN/ms_learning
     default-label: main 
     clone-on-start: true